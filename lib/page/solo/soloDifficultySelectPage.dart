import 'package:flutter/material.dart';

class SoloDifficultySelectPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Solo Difficulty Select page"),
      ),
    );
  }
}